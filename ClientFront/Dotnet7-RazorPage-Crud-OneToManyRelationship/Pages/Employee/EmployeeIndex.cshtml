@page "/employee/index"
@model dot7.razor.crudsample.Pages.Employee.EmployeeIndex

<div class="container">
    <div class="row">
        <div class="col col-md-4 offset-md-4">
            <a asp-page="./EmployeeCreate" >Add Novo Cliente</a>
        </div>
    </div>
    <div class="row">

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Nome</th>
                    <th scope="col">E-mail</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var emp in Model.AllClient)
                {
                    <tr>
                        <th>@emp.Id</th>
                        <td>@emp.Name</td>
                        <td>@emp.Email</td>
                        <td>
                            <a asp-page="./EmployeeDetails" asp-route-id="@emp.Id">Details</a>  |
                            <a asp-page="./EmployeeUpdate" asp-route-id="@emp.Id">Update</a> |
                            <a asp-page="./EmployeeDelete" asp-route-id="@emp.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>